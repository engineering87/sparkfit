## sparkfit v0.4.0

Fourth release of **sparkfit**, the LLM memory capacity planner for the NVIDIA
DGX Spark (GB10). This release focuses on model fidelity, adding co-serving and
prefill/time-to-first-token estimates, and ships sparkfit on PyPI.

### Highlights
- **Co-serving planner**: `sparkfit serve MODEL[:quant[:context]] ...` plans
  several models on one Spark at once. Their weights, KV-cache and activations sum
  against the unified 128 GB, and the shared 273 GB/s is split across the models
  decoding at the same time (worst case: each gets about its solo speed divided by
  the number active), so you see both what co-resides and how much each slows the
  others down. This matches the measured contention when co-serving on a real
  Spark.
- **Prefill / time-to-first-token**: `plan` and the quick report estimate TTFT as
  `max(compute-bound, bandwidth-bound)`, so short prompts are limited by streaming
  the weights once and long prompts by compute. New `--prompt-tokens`, plus
  `--mfu` and `--compute-tflops` (with `SPARKFIT_MFU` / `SPARKFIT_TFLOPS`) to
  calibrate the compute side to a device.
- **Quantization auto-detect**: when a model's `config.json` carries a
  `quantization_config` (FP8, GPTQ, AWQ, bitsandbytes, compressed-tensors), the
  quick report uses the detected precision as the default quant instead of
  guessing, and labels it `(detected)`. Explicit `-q` still overrides it.
- **On PyPI**: install with `pip install sparkfit` or `pipx install sparkfit`.
  Tagged releases are built and published automatically via GitHub Actions
  Trusted Publishing.

### Commands
`plan`, `advise`, `fit`, `serve`, `scan`, `models`, plus a one-shot quick mode
(pass a model name, a partial name, a Hugging Face id, or a local config path).
Every command supports `--json`.

### Install
```bash
pipx install sparkfit        # or: pip install sparkfit
sparkfit llama3.1-70b
```
Or clone and run the single file: `python src/sparkfit.py llama3.1-70b`.

### Quality
Linted with ruff, type-checked with mypy, 88 tests at about 80% coverage, CI on
Python 3.8 to 3.12. Calibrated and validated on a real DGX Spark (Qwen3.5-27B FP8
under vLLM): single-stream, concurrent and co-served decode within roughly 5 to
10 percent of measured throughput.

### Notes
These are capacity-planning estimates, not measurements; cross-check with
`sparkfit scan` on the real machine. Auto-fetch needs internet; gated Hugging Face
models need `HF_TOKEN`. The co-serving split is a worst case; idle or bursty
models free their bandwidth share.

**Full changelog**: https://github.com/engineering87/sparkfit/compare/v0.3.0...v0.4.0
